import { ButtonTooltip, Tooltip } from "../tooltip";

export function CreateMessagePage() {
    return (
        <div>
            <ButtonTooltip 
                content="Create a new message page"
                text="Get started"
            />
        </div>
    );
}